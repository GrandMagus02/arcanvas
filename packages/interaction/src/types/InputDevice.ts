/**
 * Input device types supported by the interaction system.
 */
export enum InputDevice {
  Mouse = "mouse",
  Keyboard = "keyboard",
  Pen = "pen",
  Touch = "touch",
  Pointer = "pointer",
  Controller = "controller",
}

# Matrix3x3Base

Matrix3x3Base is a base class for all 3x3 matrices.

## Static Methods

- [from](#from)
- [from](#from)
- [from](#from)

### from

| Method | Type |
| ---------- | ---------- |
| `from` | `{ <TArr extends TypedArray, TRowVec, TSelf extends Matrix3x3Base<TArr, TRowVec>>(this: { new (a11: number, a12: number, a13: number, a21: number, a22: number, a23: number, a31: number, a32: number, a33: number): TSelf; prototype: Matrix3x3Base<...>; }, rows: ArrayLike<ArrayLike<number>>): TSelf; <TArr extends TypedA...` |

### from

| Method | Type |
| ---------- | ---------- |
| `from` | `{ <TArr extends TypedArray, TRowVec, TSelf extends Matrix3x3Base<TArr, TRowVec>>(this: { new (a11: number, a12: number, a13: number, a21: number, a22: number, a23: number, a31: number, a32: number, a33: number): TSelf; prototype: Matrix3x3Base<...>; }, rows: ArrayLike<ArrayLike<number>>): TSelf; <TArr extends TypedA...` |

### from

| Method | Type |
| ---------- | ---------- |
| `from` | `{ <TArr extends TypedArray, TRowVec, TSelf extends Matrix3x3Base<TArr, TRowVec>>(this: { new (a11: number, a12: number, a13: number, a21: number, a22: number, a23: number, a31: number, a32: number, a33: number): TSelf; prototype: Matrix3x3Base<...>; }, rows: ArrayLike<ArrayLike<number>>): TSelf; <TArr extends TypedA...` |

# Uint8Matrix3x3

Uint8Matrix3x3 is a 3x3 matrix of unsigned 8-bit integers.

# Int8Matrix3x3

Int8Matrix3x3 is a 3x3 matrix of signed 8-bit integers.

# Int16Matrix3x3

Int16Matrix3x3 is a 3x3 matrix of signed 16-bit integers.

# Uint16Matrix3x3

Uint16Matrix3x3 is a 3x3 matrix of unsigned 16-bit integers.

# Float32Matrix3x3

Float32Matrix3x3 is a 3x3 matrix of 32-bit floating point numbers.

# Float64Matrix3x3

Float64Matrix3x3 is a 3x3 matrix of 64-bit floating point numbers.

# Matrix2x2Base

Matrix2x2Base is a base class for all 2x2 matrices.

## Static Methods

- [from](#from)
- [from](#from)
- [from](#from)

### from

| Method | Type |
| ---------- | ---------- |
| `from` | `{ <TArr extends TypedArray, TRowVec, TSelf extends Matrix2x2Base<TArr, TRowVec>>(this: { new (a11: number, a12: number, a21: number, a22: number): TSelf; prototype: Matrix2x2Base<...>; }, rows: ArrayLike<ArrayLike<number>>): TSelf; <TArr extends TypedArray, TRowVec, TSelf extends Matrix2x2Base<TArr, TRowVec>>(this: ...` |

### from

| Method | Type |
| ---------- | ---------- |
| `from` | `{ <TArr extends TypedArray, TRowVec, TSelf extends Matrix2x2Base<TArr, TRowVec>>(this: { new (a11: number, a12: number, a21: number, a22: number): TSelf; prototype: Matrix2x2Base<...>; }, rows: ArrayLike<ArrayLike<number>>): TSelf; <TArr extends TypedArray, TRowVec, TSelf extends Matrix2x2Base<TArr, TRowVec>>(this: ...` |

### from

| Method | Type |
| ---------- | ---------- |
| `from` | `{ <TArr extends TypedArray, TRowVec, TSelf extends Matrix2x2Base<TArr, TRowVec>>(this: { new (a11: number, a12: number, a21: number, a22: number): TSelf; prototype: Matrix2x2Base<...>; }, rows: ArrayLike<ArrayLike<number>>): TSelf; <TArr extends TypedArray, TRowVec, TSelf extends Matrix2x2Base<TArr, TRowVec>>(this: ...` |

# Uint8Matrix2x2

Uint8Matrix2x2 is a 2x2 matrix of unsigned 8-bit integers.

# Int8Matrix2x2

Int8Matrix2x2 is a 2x2 matrix of signed 8-bit integers.

# Int16Matrix2x2

Int16Matrix2x2 is a 2x2 matrix of signed 16-bit integers.

# Uint16Matrix2x2

Uint16Matrix2x2 is a 2x2 matrix of unsigned 16-bit integers.

# Float32Matrix2x2

Float32Matrix2x2 is a 2x2 matrix of 32-bit floating point numbers.

# Float64Matrix2x2

Float64Matrix2x2 is a 2x2 matrix of 64-bit floating point numbers.

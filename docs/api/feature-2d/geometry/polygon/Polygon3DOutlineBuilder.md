# Polygon3DOutlineBuilder

Builder that creates an outline from a 3D polygon shape (no triangulation).
Creates line indices to draw a closed loop outline.

## Methods

- [build](#build)

### build

| Method | Type |
| ---------- | ---------- |
| `build` | `(shape: Shape3D) => MeshBuildResult` |
